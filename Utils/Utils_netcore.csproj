<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>Utils</AssemblyName>
    <TargetFrameworks>net10.0;net48</TargetFrameworks>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>
  <ItemGroup Condition="'$(TargetFramework)' != 'net48'">
    <!-- for .NET (Core) -->
    <!-- WinBased is included so net10.0 consumers (e.g. EDE) can use ComputerSystemHardwareHelper.
         Callers must only invoke WMI helpers on Windows (see OS guard in GetGammaSourceAndSerial). -->
    <PackageReference Include="System.IO.Ports" Version="8.0.0" />
    <PackageReference Include="System.IO.Packaging" Version="10.0.2" />
    <PackageReference Include="System.Management" Version="8.0.0" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetFramework)' == 'net48'">
    <!-- for .NET Framework 4.8 -->
    <Reference Include="System.Management" />
    <Reference Include="WindowsBase" />
  </ItemGroup>
</Project>
